# ProbeMate shared contracts

The MVP keeps duplicated TypeScript and Pydantic schemas small and explicit. This folder is reserved for schema snapshots that should remain stable across frontend and API work.
